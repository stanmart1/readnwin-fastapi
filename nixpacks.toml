[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[phases.start]
cmd = "npm start"

[healthcheck]
cmd = "echo 'healthy'"
interval = "30s"
timeout = "10s"
retries = 3
